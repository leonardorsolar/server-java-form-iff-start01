package com.example.server_java_form_iff_start;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import com.example.server_java_form_iff_start.Conta.Conta;

public class ContaTeste {
    @Test
    void testCriarConta() {
        // Deve criar uma conta com saldo zero
        // Given: dado que
        // Aqui você deve criar qualquer configuração necessária para o teste.

        // When: Quando
        // Aqui você deve realizar a ação ou evento que está sendo testado.

        // Then: Então
        // Aqui você deve verificar se o resultado é o esperado.
        // assertEquals(0, 0, "O saldo inicial da conta deve ser zero");
        assertEquals(0, variavel);
    }
}
